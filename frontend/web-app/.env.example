# Frontend environment variables for ROIMSG web app
# Copy this file to ".env.local" and fill the values.

# API Gateway base URL
VITE_API_BASE_URL=http://localhost:8080

# Google OAuth (Client-side)
VITE_GOOGLE_CLIENT_ID=
VITE_GOOGLE_REDIRECT_URI=http://localhost:3000/auth/callback
# Typically remains default; change only if needed
VITE_GOOGLE_AUTH_URI=https://accounts.google.com/o/oauth2/v2/auth
